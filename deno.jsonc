{
    "tasks": {
        "deploy": "deno run --allow-read --allow-write --allow-env installer.ts deploy run",
        "setup": "deno run --allow-read --allow-write --allow-env installer.ts setup run"
    }
}
